"""
Lightweight package wrapper for ReLoop.

The codebase historically lived at the repository root (e.g., `solvers/`,
`eval/`). This namespace package allows new agent and benchmarking modules to
reside under `reloop.*` while still importing existing top-level modules.
"""

